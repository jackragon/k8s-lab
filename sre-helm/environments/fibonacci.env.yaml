env:
  - name: SERIES_VERSION
    value: fibonacci
  - name: VAR2
    value: var2_value
  - name: VAR3
    value: this_is_var3_value    

#use this command:  helm upgrade --install fibonacci ./sre-helm -f ./sre-helm/environments/fibonacci.yaml -f ./sre-helm/environments/fibonacci.env.yaml 